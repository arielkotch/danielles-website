import { theme } from '../../../theme';

export const styles = {
  header: {
    fontSize: '4em',
    ...theme.fontFamily,
  },
  paragraph: {
    fontSize: '1.33em',
    ...theme.fontFamily,
  },
  segment: { marginBottom: '2em', marginTop: '2em', padding: '8em 0em' }
};
