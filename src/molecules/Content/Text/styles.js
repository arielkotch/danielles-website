import { theme } from '../../../theme';

export const styles = {
  header: {
    fontSize: '2em',
    ...theme.fontFamily,
  },
  paragraph: {
    fontSize: '1.33em',
    ...theme.fontFamily,
  },
  segment: { padding: '8em 0em' }
};
