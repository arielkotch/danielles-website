import { theme } from '../../theme';

export const styles = {
  brand: {
    fontSize: '3em',
    ...theme.fontFamily,
  },
  menuItem: {
    fontSize: '1.4em',
    'margin-top': '1.6em',
    ...theme.fontFamily,
  }
};
