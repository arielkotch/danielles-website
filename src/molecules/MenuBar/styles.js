import { theme } from '../../theme';

export const styles = {
  brand: {
    'margin-top': '0em',
    fontSize: '3em',
    ...theme.fontFamily,
  },
  menuItem: {
    fontSize: '1.4em',
    'margin-top': '1.5em',
    ...theme.fontFamily,
  }
};
